<%= form_for(@venue_travel_detail, :html => {:class => "form-horizontal"}) do |f| %>
  <% if @venue_travel_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_travel_detail.errors.count, "error") %> prohibited this venue_travel_detail from being saved:</h2>

      <ul>
      <% @venue_travel_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :venue_id %>
    <div class="controls"><%= collection_select(:venue_travel_detail, 
                          :venue_id, 
                          Venue.all, 
                          :id, 
                          :name, 
                          :prompt => 'Select a venue') %></div>
  </div>
  <div class="control-group">
    <%= f.label :method %>
    <div class="controls"><%= f.text_field :method %></div>
  </div>
  <div class="control-group">
    <%= f.label :details %>
    <div class="controls"><%= f.text_area :details %></div>
  </div>
  <div class="form-actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
