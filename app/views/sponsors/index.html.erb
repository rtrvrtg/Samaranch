<h1>Listing Sponsors</h1>

<table class="table table-bordered table-striped">
<thead>
  <tr>
    <th>Name</th>
    <th>Sponsor type</th>
    <th>Location</th>
    <th colspan="3"></th>
  </tr>
</thead>
<tbody>
<% @sponsors.each do |sponsor| %>
  <tr>
    <td><%= sponsor.name %></td>
    <td><%= sponsor.sponsor_type.title %></td>
    <td><%= sponsor.location.name %></td>
    <td><%= link_to 'Show', sponsor %></td>
    <td><%= link_to 'Edit', edit_sponsor_path(sponsor) %></td>
    <td><%= link_to 'Destroy', sponsor, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</tbody>
<tfoot>
	<tr><td colspan="6">
		<%= link_to 'New Sponsor', new_sponsor_path %>
	</td></tr>
</tfoot>
</tbody>
</table>


