<%= form_for(@sponsor_type, :html => {:class => "form-horizontal"}) do |f| %>
  <% if @sponsor_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sponsor_type.errors.count, "error") %> prohibited this sponsor_type from being saved:</h2>

      <ul>
      <% @sponsor_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :title %>
    <div class="controls"><%= f.text_field :title %></div>
  </div>
  <div class="control-group">
    <%= f.label :description %>
    <div class="controls"><%= f.text_area :description %></div>
  </div>
  <div class="form-actions">
    <%= f.submit :class => 'btn btn-primary' %>
  </div>
<% end %>
